@layout('main')
@section('content')
    <a href="/posts">Go Back</a>
    <h1>{{post.title}}</h1>
    <p>{{post.body}}</p>
<a href="/posts/edit/{{post.id}}" class="btn btn-dark">Edit</a>
<form action="{{'/posts/'+post.id+'?_method=DELETE'}}" method="POST"></form>
<form action="{{ '/posts/' + post.id + '?_method=DELETE' }}" method="POST">
    {{ csrfField() }}
    <button type="submit" class="btn btn-danger d-block float-right">Delete</button>
  </form>
@endsection